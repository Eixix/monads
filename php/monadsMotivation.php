<?php

class IntegerWithLogs
{
  public int $result;
  public array $logs;

  public function __construct(int $result, array $logs)
  {
    $this->result = $result;
    $this->logs = $logs;
  }

  public function __toString()
  {
    return json_encode($this->logs);
  }
}

class Calculate
{
  public static function square(int $x): IntegerWithLogs
  {
    return new IntegerWithLogs($x * $x, ["Squared $x to get " . ($x * $x)]);
  }

  public static function addOne(int $x): IntegerWithLogs
  {
    return new IntegerWithLogs($x + 1, ["Added 1 to $x to get " . ($x + 1)]);
  }

  // TODO: Add the following capabilities
  // FIXME: Not working Calculate::square(Calculate::addOne(2))
  // Don't use if statements or change the IntegerWithLogs class, 
  // you may need 1 or 2 helper functions in Calculate
  // you may not alter the methods square or addOne
  // One hint function is way below at line 1000
}

echo (Calculate::square(2));































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































/*
  # You need one more function   
  public static function wrapWithLogs(int $x): IntegerWithLogs
  {
    return new IntegerWithLogs($x, []);
  }

*/
